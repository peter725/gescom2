import { B64FileAccessorDirective } from './b64-file-accessor.directive';

describe('B64FileAccessorDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new B64FileAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
