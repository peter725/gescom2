import { IsSameSubjectDirective } from './is-same-subject.directive';

describe('IsSameSubjectDirective', () => {
  it('should create an instance', () => {
    const directive = new IsSameSubjectDirective();
    expect(directive).toBeTruthy();
  });
});
