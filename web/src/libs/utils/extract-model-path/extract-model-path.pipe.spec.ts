import { ExtractModelPathPipe } from './extract-model-path.pipe';

describe('ExtractModelPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractModelPathPipe();
    expect(pipe).toBeTruthy();
  });
});
